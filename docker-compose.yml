version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: draftly
      POSTGRES_PASSWORD: draftlypass
      POSTGRES_DB: draftlydb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
